# Unified dependency file for the entire IREPS Tenders project.
# Install from the repository root with: pip install -r requirements.txt
#
# Python 3.13 notes:
# - Older exact pins pulled in packages without cp313 wheels, especially the
#   legacy TensorFlow/Keras OCR stack. The current scraper imports the PyTorch
#   CAPTCHA solver in Scraping/Program_Files/captcha_solver.py, so TensorFlow and
#   keras-ocr are intentionally not installed here.
# - Version ranges keep the environment reproducible while still allowing pip to
#   choose Python 3.13-compatible wheels.

beautifulsoup4>=4.12,<5
chromedriver-autoinstaller>=0.6,<1
customtkinter>=5.2,<6
dash>=3.0,<4
dash-bootstrap-components>=2.0,<3
numpy>=2.1,<3
openpyxl>=3.1,<4
pandas>=2.2.3,<3
pdfplumber>=0.11,<1
Pillow>=11,<13
plotly>=6.0,<7
pyinstaller>=6.13,<7
pyinstaller-hooks-contrib>=2025.1,<2027
python-dateutil>=2.9,<3
requests>=2.32,<3
selenium>=4.27,<5
torch>=2.9,<3
torchvision>=0.24,<1
XlsxWriter>=3.2,<4
